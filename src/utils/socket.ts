// import { IUser } from "./types";

// export default function connectSocket(user: IUser) {
    // const socket = io("ws://localhost:3000", { query: { userId: user.id } });

    // socket.on("connection", data => {
    //     console.log(data);
    // });

    // socket.emit("new-message", { })
// }

